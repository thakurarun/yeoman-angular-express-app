'use strict';

/**
 * @ngdoc function
 * @name chatwebApp.controller:UserCtrl
 * @description
 * # UserCtrl
 * Controller of the chatwebApp
 */
angular.module('chatwebApp')
  .controller('UserCtrl', function () {
    this.awesomeThings = [
      'HTML5 Boilerplate',
      'AngularJS',
      'Karma'
    ];
  });
